# Unity Coursework Space Shooter

Public coursework project containing Unity project files and a Windows release build.

## Contents

- `project-files/` - Unity project source files and assets
- `release-build/` - Windows build output

Unity generated folders such as `Library`, `Temp`, `Logs`, and `obj` are intentionally excluded from the repository.
